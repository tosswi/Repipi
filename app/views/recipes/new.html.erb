<%= form_for (@recipe) do |f| %>
    <% if @recipe.errors.any? %>
        <%= @recipe.errors.count %>件のerrorが発生しました
        <% @recipe.errors.full_messages.each do |message| %>
            <%= message %>
        <% end %>
    <% end %>


       <%= form_for @recipe, html: { multipart: true } do |recipe|%>
        <%= recipe.fields_for :@recipe do |f| %>
            <%= f.file_field :image, multiple: "true" %>
        <% end %>
        <%= recipe.submit %>
    <% end %>


  <div class="container">
      <div class="row">
        <div class="col-xs-2">
        <%= f.label :image, "画像選択", class: "image_input_btn btn-primary" %>
        <%= f.attachment_field :image, class: "image_form" %>
        <div class="field row">
          <%= f.label :レシピ名 %>
          <%= f.text_field :name,:placeholder => "チャーハン" %>
        </div>
        <div class="field row">
          <%= f.label :材料 %>
          <%= f.text_area :material ,:placeholder => "卵：2個、ネギ一本、オイスターソース：大さじ1" %>
        </div>
        <div class="field row">
          <%= f.label :human %>
          <%= f.select :human ,Recipe.humen.keys.to_a, {}%>
        </div>
        <td><%= f.collection_select :genre_id, @genres, :id, :name %></td>
         <td><%= f.collection_select :category_id, @categories, :id, :name %></td>
      </div>
      <div class="col-xs-10">
        <div class="field row">
          <%= f.label :content %>
          <%= f.text_area :content,:placeholder => "作り方", :size=>"60x10" %>
        </div>
        <div class="actions row">
          <%= f.submit class: "btn btn-primary col-xs-12" %>
        </div>
     </div>
  </div>
<% end %>